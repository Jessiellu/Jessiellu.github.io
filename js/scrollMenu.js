window.addEventListener('load', function() {
    //获取顶部菜单元素
    let navBar = document.querySelector('#header__nav')
    //获取元素高度
    let navBarHeight = navBar.offsetHeight
    //获取顶部菜单后面的内容，为此添加样式
    let afterContent = document.querySelector('#header__title')
    //获取顶部菜单距离页面顶部的距离
    let navBarTop = navBar.offsetTop

    //添加页面滚动的监听：
})